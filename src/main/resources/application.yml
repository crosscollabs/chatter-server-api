spring:
  datasource:
    url: jdbc:mariadb://localhost:3306/chatter
    username: chatter
    password: chatter
    driver-class-name: org.mariadb.jdbc.Driver
  jpa:
    generate-ddl: true
    show-sq: true
server:
  port: 8081

  